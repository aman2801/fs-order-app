Simple Node.js App - Lightweight Secure Version (TXT)
====================================================

✅ Simplified Tech Stack
-----------------------
Frontend:       React.js/Next.js or EJS
Backend:        Node.js + Express.js
Database:       MongoDB Atlas (Free Tier)
Auth:           JWT with email/password
Session:        JWT with 24h expiry
Hosting:        Render/Vercel/Railway
File Storage:   Local with size limits

🔒 Security Enhancements
-----------------------
1. Rate limiting on auth endpoints
2. JWT with 24h expiry + refresh tokens
3. Input sanitization middleware
4. Helmet.js for HTTP headers
5. CORS restrictions
6. Environment variables for all secrets

🧩 Simplified App Flow
----------------------

1. User Registration & Login
----------------------------
- User provides email and password
- Backend validates and stores hashed password (bcrypt)
- Returns JWT (24h expiry) + refresh token
- Refresh token allows getting new JWT without re-login

2. Product Catalog
------------------
- Authenticated GET request to /products
- Returns paginated product list
- Optional filters via query params

3. Order Placement
------------------
- Authenticated POST to /orders
- Stores order in MongoDB with:
  - User reference
  - Timestamp
  - IP address
  - Order status (pending)

📦 APK Options (Unchanged)
--------------------------
Option 1: WebView in Android Studio
Option 2: React Native App

✅ Simplified API Endpoints
--------------------------
POST   /auth/register     → Create user account
POST   /auth/login        → Login and get tokens
POST   /auth/refresh      → Get new JWT with refresh token
GET    /products          → List products (auth required)
POST   /orders            → Place new order
GET    /orders            → Get user's order history

⚡ Optimizations
---------------
1. Lightweight middleware stack
2. Connection pooling for MongoDB
3. Compressed responses
4. Cache headers for product data
5. Minimal dependencies
6. Error handling middleware

📝 Sample .env
--------------
NODE_ENV=development
PORT=3000
MONGODB_URI=your_connection_string
JWT_SECRET=complex_secret_here
JWT_EXPIRES_IN=24h
REFRESH_SECRET=another_secret
REFRESH_EXPIRES_IN=7d
RATE_LIMIT_WINDOW=15m
RATE_LIMIT_MAX=100